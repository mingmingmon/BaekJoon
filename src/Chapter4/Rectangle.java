/*
package Chapter4;
import java.util.Scanner;
public class Rectangle {
    int x,y,width,height;
    Rectangle(int x, int y, int width, int height) {
        this.x = x;
        this.y = y;
        this.width = width;
        this.height = height;
    }
    int square() {
        return width*height;
    }
    void show() {
        System.out.println("사각형의 좌표는 " + x + "," + y + "이고 넓이는 " + this.square() + "입니다.");
    }
    boolean contains(Rectangle r) {
        if(r.x < this.x && r.y < this.y && r.width + r.x < this.width + this.x && r.y + r.height < this.y + this.height) {

        }
    }
}
*/
